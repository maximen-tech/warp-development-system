# Warp Development System - Environment Variables
# Copy this file to .env and add your actual keys

# Anthropic Claude API (for Prompt Factory AI synthesis)
# Get your key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-your_actual_key_here

# Optional: OpenAI API (future integration)
# OPENAI_API_KEY=sk-your_openai_key_here

# Server Configuration
PORT=3030
NODE_ENV=development

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit the actual .env file to git
# 2. NEVER share API keys in chat/email/slack
# 3. Rotate keys regularly (every 90 days)
# 4. Use separate keys for dev/staging/production
# 5. If a key is exposed, revoke it IMMEDIATELY