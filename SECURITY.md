# Security Policy

- Report vulnerabilities privately via security@yourdomain.tld
- Do not include secrets in issues or PRs; use placeholders like {{TOKEN_NAME}}
- We rotate tokens if exposure is suspected
