# Common Issues (FAQ & Troubleshooting)

- Agent proposes large diffs → tighten scope, attach files
- Secrets leaked in logs → use placeholders {{TOKEN_NAME}}
- Slow runs → enable caching, parallelize, route planning to DeepSeek
- Failing CI checks → run /review locally to see lint/test failures
