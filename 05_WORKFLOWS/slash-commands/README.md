# Slash Commands

Each command folder includes:
- README.md (usage and parameters)
- command.sh (bash)
- command.ps1 (PowerShell)

Available commands
- deploy, debug, test, audit, plan, build, review, performance, incident, migrate, document, sync, optimize
