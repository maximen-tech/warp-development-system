# /autopilot

Usage
- bash: `bash autopilot.sh [cycles]`
- pwsh: `./autopilot.ps1 -Cycles 3`

Purpose
- Run N autopilot cycles (plan/execute/validate). Stops for critical actions; recommends safest choice first.
