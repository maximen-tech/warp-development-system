# Agent Profiles and Skills

This directory defines reusable skills and agent profiles.

- skills.yml — atomic capabilities with inputs/outputs and guardrails
- agents.yml — named agents with role, model routing, and allowed skills

Use agent-sync command to validate and print active config.
